function raceProgression() {
  /** TODO: 
   * 1. 매개 변수로 레이스 초기 데이터, 시행 횟수를 받는다
   * 2. 레이스 결과를 저장할 레이스 데이터를 선언
   * 2. 레이스 초기 데이터의 키값을 배열로 선언 [...레이서.keys()]
   * 3. 
   * . 반복문을 활용하여 시행 횟수와 레이서의 수만큼 레이스 결과를 실행한다
   *  - goStopResult의 결과를 변수에 저장
   *  - 해당 결과를 레이스 데이터에 set
   *  - 한 사이클을 돌은 결과를 출력
   * 9. 레이스 결과를 반환
   */
};