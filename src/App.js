import { Random, Console } from "@woowacourse/mission-utils";
// Random.pickNumberInRange() : Random 값 추출
// Console.readLineAsync() : 사용자 값 입력
// Console.print() : 사용자 값 출력

// Random.pickNumberInRange(0, 9);   : 0에서 9까지의 정수 중 한 개의 정수 반환

class App {
  async run() {}
}

export default App;
