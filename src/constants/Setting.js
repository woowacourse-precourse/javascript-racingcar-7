export const RANDOM_SETTING = Object.freeze({
    MIN_NUMBER: 0,
    MAX_NUMBER: 9,
});

export const MOVE_SETTING = Object.freeze({
    MOVE_POSSIBLE_NUMBER: 4,
    UNIT_PER_RACE: 1,
    DEFAULT_POSITION: 0,
});

export const PRINT_SETTING = Object.freeze({
    LOG_JOIN_SEPARATOR: "\n",
    WINNER_JOIN_SEPARATOR: ", ",
});

export const POSITION_MARKER = Object.freeze("-");

export const CAR_NAME_DELEMETER = Object.freeze(",");
