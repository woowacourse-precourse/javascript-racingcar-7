/**
 * 자동차가 이동할 수 있는 임계값(난수 값이 이 값 이상일 때 자동차가 전진한다)
 *
 * @constant {number}
 */
export const MOVE_THRESHOLD = 4;

/**
 * 게임의 최대 시도 횟수
 *
 * @constant {number}
 */
export const MAX_ROUNDS = 100;

/**
 * 자동차 이름의 최대 길이
 *
 * @constant {number}
 */
export const MAX_CAR_NAME_LENGTH = 5;
