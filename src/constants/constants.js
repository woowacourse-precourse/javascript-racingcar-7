const INIT_NUM = 0;

const MIN_MOVABLE_NUM = 4;

const MOVE_INCREMENT = 1;

const MIN_CAR_NAME_LEN = 1;

const MAX_CAR_NAME_LEN = 5;

const MIN_TRY_COUNT = 1;

const MAX_TRY_COUNT = 300;

const MIN_RANDOM_NUM = 0;

const MAX_RANDOM_NUM = 9;

export {
  INIT_NUM,
  MIN_MOVABLE_NUM,
  MOVE_INCREMENT,
  MIN_CAR_NAME_LEN,
  MAX_CAR_NAME_LEN,
  MIN_TRY_COUNT,
  MAX_TRY_COUNT,
  MIN_RANDOM_NUM,
  MAX_RANDOM_NUM,
};
