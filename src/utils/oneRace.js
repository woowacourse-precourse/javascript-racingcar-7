import { Random } from "@woowacourse/mission-utils"
import makeRandom from "./MakeRamdom"

export const oneRace = (names)=>{
    ///전진하는 비열Random.pickNumberInRange(0,9)
    names.map(name=>{
        const go = Random.pickNumberInRange(0,9)
        if(go>=4){
            
        }
    })

}